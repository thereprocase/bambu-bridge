# Server
BRIDGE_HOST=0.0.0.0
BRIDGE_PORT=8080
BRIDGE_API_KEY=             # generate with `openssl rand -hex 32`

# Storage
BRIDGE_DB_PATH=/var/lib/bambu-bridge/jobs.db
BRIDGE_FILES_DIR=/var/lib/bambu-bridge/files
# Buffered file upload/download ceiling (64 MiB); size for available container RAM.
BRIDGE_MAX_TRANSFER_BYTES=67108864

# Logging
BRIDGE_LOG_LEVEL=info
BRIDGE_LOG_FORMAT=json

# Push (optional)
NTFY_URL=https://ntfy.sh
NTFY_TOPIC=
